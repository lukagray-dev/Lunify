package com.android.lunify.videoplayer.engine.manager

// Legacy video-engine factory removed. ExoPlayer is bundled directly and does
// not expose a runtime switcher or per-engine settings UI anymore.
