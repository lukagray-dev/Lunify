package com.android.lunify.videoplayer.engine.manager

// Legacy video-engine management removed. ExoPlayer is used directly by the
// app, so the replaceable manager/factory stack is no longer part of runtime.
