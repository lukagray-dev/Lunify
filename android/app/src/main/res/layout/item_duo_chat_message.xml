<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingVertical="4dp"
    android:paddingHorizontal="8dp">

    <!-- Outgoing Message Container (Right) -->
    <LinearLayout
        android:id="@+id/containerOutgoing"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_marginStart="64dp"
        android:background="@drawable/bg_chat_bubble_outgoing"
        android:orientation="vertical"
        android:paddingHorizontal="12dp"
        android:paddingVertical="8dp"
        android:visibility="gone"
        tools:visibility="visible">

        <TextView
            android:id="@+id/tvOutgoingText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@android:color/white"
            android:textSize="14sp"
            tools:text="Hello, this is my outgoing message!" />

        <!-- Voice Outgoing Container -->
        <LinearLayout
            android:id="@+id/voiceOutgoingContainer"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            android:visibility="gone">

            <ImageView
                android:id="@+id/ivOutgoingVoicePlay"
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:src="@drawable/ic_play"
                app:tint="@android:color/white" />

            <TextView
                android:id="@+id/tvOutgoingVoiceDuration"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="8dp"
                android:textColor="@android:color/white"
                android:textSize="12sp"
                android:textStyle="bold"
                tools:text="Voice (0:03)" />

        </LinearLayout>

        <TextView
            android:id="@+id/tvOutgoingStatus"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="end"
            android:layout_marginTop="2dp"
            android:textColor="#CCCCCC"
            android:textSize="9sp"
            tools:text="Read" />

    </LinearLayout>

    <!-- Incoming Message Container (Left) -->
    <LinearLayout
        android:id="@+id/containerIncoming"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_marginEnd="64dp"
        android:background="@drawable/bg_chat_bubble_incoming"
        android:orientation="vertical"
        android:paddingHorizontal="12dp"
        android:paddingVertical="8dp"
        android:visibility="gone"
        tools:visibility="visible">

        <!-- Sender name -->
        <TextView
            android:id="@+id/tvIncomingSender"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/colorAccent"
            android:textSize="11sp"
            android:textStyle="bold"
            tools:text="Partner Device" />

        <TextView
            android:id="@+id/tvIncomingText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:textColor="@color/textPrimary"
            android:textSize="14sp"
            tools:text="Hello, this is an incoming message!" />

        <!-- Voice Incoming Container -->
        <LinearLayout
            android:id="@+id/voiceIncomingContainer"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            android:visibility="gone">

            <ImageView
                android:id="@+id/ivIncomingVoicePlay"
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:src="@drawable/ic_play"
                app:tint="@color/textPrimary" />

            <TextView
                android:id="@+id/tvIncomingVoiceDuration"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="8dp"
                android:textColor="@color/textPrimary"
                android:textSize="12sp"
                android:textStyle="bold"
                tools:text="Voice (0:03)" />

        </LinearLayout>

    </LinearLayout>

</RelativeLayout>
